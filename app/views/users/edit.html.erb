<% if logged_in? %>
<div class="container">
    <div class="py-4 text-center">
        <h1>Edit User</h1>
    </div>

    <%= render 'form' %>

    <%= link_to 'Back', user_path(@user) %>
</div>
<% else %>
     <%= render 'errors/not_signed_in' %>
<% end %>