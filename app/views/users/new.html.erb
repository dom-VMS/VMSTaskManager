<!-- Create new User form -->
<% if logged_in? && isAdmin? %>
    <h1>New User</h1>

    <%= render 'form' %>

    <%= link_to 'Back', users_path %>
<% else %>
     <%= render 'errors/not_signed_in' %>
<% end %>